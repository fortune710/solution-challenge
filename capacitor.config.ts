import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solution.challenge',
  appName: 'solution-challenge',
  webDir: 'dist/solution-challenge',
  bundledWebRuntime: false
};

export default config;
