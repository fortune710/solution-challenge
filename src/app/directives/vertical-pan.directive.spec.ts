import { VerticalPanDirective } from './vertical-pan.directive';

describe('VerticalPanDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalPanDirective();
    expect(directive).toBeTruthy();
  });
});
